# 内存占用优化分析

基于 **TIKTOK_SOUSUO_YANGHAO.js**、**AutoJs 源码** 和 **PSS内存指标.md** 的结论与可落地的优化建议。

---

## 一、PSS 与可优化方向（回顾）

- **PSS** = 系统视角下本进程占用的物理内存（Java 堆 + Native 堆 + 共享库均摊 + 栈/图形等）。
- 能主动控制的主要是：
  - **Java 堆**：少留大对象、控制缓存/集合大小、及时释放引用。
  - **Native 堆**：截图/找图等用到的 Image/OpenCV 等，用后及时 recycle/release。
- 下面从「脚本」「AutoJs 源码」以及「共享库」三方面说明可做的优化。

---

## 二、脚本层（TIKTOK_SOUSUO_YANGHAO.js）

### 已做得好的

- **截图后回收**：`Nest_ScreenCapture()` 里 `img.saveTo(path)` 后调用了 `img.recycle()`，能降低 Native 位图占用，值得保留。

### 建议优化

1. **去掉重复的 `requestScreenCapture()`**  
   `Nest_ScreenCapture()` 里对 `requestScreenCapture()` 调用了两次，无必要，且可能多一次权限/逻辑开销。保留一次即可。

2. **控制 `fail_msg` 体积**  
   `handleError()` 里用 `fail_msg += ...` 不断追加，异常多次时字符串会很大，增加 Java 堆与最终写入内容。  
   - 可对 `fail_msg` 做长度上限（例如只保留最近 N 条或前 5000 字符），超出部分截断或只保留条数统计。

3. **大文件读取方式**  
   `get_all_comments()` / `get_all_keyword()` 用 `BufferedReader.readLine()` 把整文件读进数组。若文件很大，会一次性占较多堆内存。  
   - 若业务允许，可改为「按行流式处理」或「只读前 N 行」；若必须全量，至少避免在脚本里同时持有多份完整拷贝。

4. **循环内少留大引用**  
   `watch_TT_video()` 里每轮都会 `Nest_ScreenCapture()`，当前已 `recycle()`，没问题。  
   - 其它地方若有 `className(...).find()` 等得到大集合，用完后尽量把引用置空或缩小作用域，便于 GC（例如不要长期挂在全局变量上）。

5. **exit 里去掉 sleep(1000)**  
   与 TIKTOK_LIULAN_YANGHAO 一样，若在 exit 回调里还有 `sleep(1000)`，在强制停止时可能抛异常导致后续逻辑不执行；且会拖长进程存活时间。建议去掉或包在 try-catch 里，避免无谓占用。

---

## 三、共享库（PSS 里的「共享」部分）能优化吗？

**可以，但空间有限，且多是架构级手段。**

- **共享库在 PSS 里是什么**：系统把进程映射的 .so、系统库（如 libc、ART）等按「共享进程数」均摊后算进 PSS。这部分不是你在代码里 malloc 出来的，而是**加载了哪些 native 库、映射了多少代码/数据页**决定的。
- **能做的优化**：
  1. **少加载 / 晚加载 native 库**：  
     只有用到的 .so 才会被加载。若 AutoJs 或依赖里有可选 native 模块（例如某图像库、加解密库），不用就不引入；若支持延迟加载，就等用到再加载，避免启动时就占一份 PSS。
  2. **去掉不必要的 native 依赖**：  
     审查 jniLibs、第三方 SDK：能删的 .so 删掉（例如只保留当前 ABI、去掉未使用的功能库），共享库部分的 PSS 会少一些。
  3. **多进程隔离**：  
     把耗 native 的模块放到独立进程（如 :heavy 进程），主进程不加载这些 .so，主进程的 PSS 会下降；整机占用可能不变甚至略增，但有利于系统按进程杀/保活。
  4. **系统库无法减**：  
     libc、ART、系统 framework 等是进程必备的，应用无法减少这部分映射。
- **小结**：共享库可以靠「少用、晚用、多进程」做一定优化，但无法像 Java/Native 堆那样通过回收、缩缓存明显下降；通常作为辅助手段。

---

## 四、AutoJs / App 源码层

### 1. 控制全局日志条数（对 PSS/Java 堆有帮助）

- **现状**：`ConsoleImpl` 里 `maxLines = -1`，即不限制条数，`mLogEntries` 会一直增长，长时间运行或刷日志多时，会多占 Java 堆并间接推高 PSS。
- **建议**：为全局 Console 设一个默认上限（例如 2000～5000 条），超过时按 FIFO 删旧条目或整体 `clear()` 再写新日志（与现有 `maxLines>0` 时逻辑一致）。  
- **实现思路**：在创建/注入 `GlobalConsole`（或对应 `ConsoleImpl`）的地方，调用 `setMaxLines(2000)`（具体数值可配置）；或修改 `ConsoleImpl` 的默认 `maxLines` 从 -1 改为 2000。

### 2. 已存在的内存相关逻辑（保持即可）

- **App.onTrimMemory / onLowMemory**：已调用 `Explorers.workspace().clearPageCache()`、`Explorers.external().clearPageCache()` 和 `Runtime.getRuntime().gc()`，有助于在系统压力大时释放缓存和堆。
- **ScriptRuntime.onExit**：已对 `mMedia`、`mImages`、threads、events、loopers、timers、ui 等做 recycle/清理，脚本结束时会释放相关资源。
- **懒加载**：Images、Gmlkit、Media、Plugins 等已按需创建，避免未使用就占大量 Native/堆。

### 3. 可选的进一步优化（需改源码）

- **脚本引擎**：若 Rhino 或脚本层有「编译结果/ AST / 作用域」的长期缓存，可考虑在脚本执行结束后或 onLowMemory 时清理或缩小缓存（需看具体实现再动）。
- **Explorer 列表**：除 `clearPageCache()` 外，若内存仍吃紧，可评估是否在 onTrimMemory 时对列表数据做更激进的释放或按需重新加载。

---

## 五、小结：优先可做的几件事

| 类型       | 项目                     | 作用                         |
|------------|--------------------------|------------------------------|
| 脚本       | 去掉一次 requestScreenCapture | 减少重复逻辑与潜在开销       |
| 脚本       | 限制 fail_msg 长度       | 降低 Java 堆与写文件体积     |
| 脚本       | 大文件流式/限行读取      | 避免一次性大数组占堆         |
| 脚本       | exit 中去掉/保护 sleep   | 避免异常与无谓延长进程存活   |
| AutoJs 源码 | 为 GlobalConsole 设 maxLines | 限制 mLogEntries 增长，控 PSS/堆 |

以上都不改 PSS 的定义与计算方式（仍以系统为准），只是在「应用层 + 脚本层」减少不必要的 Java/Native 占用，从而在整体上有利于降低 PSS 和内存压力。
